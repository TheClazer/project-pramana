# tools/cli_sealverify — runtime + tests
cryptography>=42.0.0     # ECDSA-P256 sign + verify
jcs>=0.2.1               # RFC 8785 JSON canonicalization
piexif>=1.1.3            # EXIF UserComment read / write
pillow>=10.0.0           # JPEG codec
numpy>=1.26.0            # DCT math
scipy>=1.12.0            # 2D DCT (drop-in for our 8×8 textbook)
pytest>=8.0.0
