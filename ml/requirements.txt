# Pramāṇa — Python ML pipeline + AI Hub Workbench scripts
#
# Tested with Python 3.11 on Windows. Compatible with 3.10/3.12.
# For GPU training prefer Colab T4 (free tier handles MobileNet-V3-Small
# in ~3-4 hours for the full FF++ + Celeb-DF + IIIT-CFW training).

# ---- Core deep learning -----------------------------------------------------
torch>=2.2.0,<2.5.0
torchvision>=0.17.0,<0.20.0
timm>=1.0.7                           # pretrained backbones (MobileNet-V3, EfficientNet-B0)
numpy>=1.26.0,<2.0.0
pillow>=10.0.0
opencv-python>=4.9.0,<5.0.0
scikit-learn>=1.4.0                   # AUC, accuracy, train/test split
tqdm>=4.66.0
einops>=0.8.0

# ---- Qualcomm AI Hub Workbench ----------------------------------------------
qai-hub>=0.30.0                       # `qai-hub configure --api_token ...` first
qai-hub-models>=0.30.0                # ships MobileNet-V3-Small / EfficientNet-B0 export helpers

# ---- rPPG prototype ---------------------------------------------------------
scipy>=1.12.0                         # signal filtering + FFT
mediapipe>=0.10.14                    # Face Mesh for ROI patches

# ---- TFLite reference / inference -------------------------------------------
tensorflow>=2.14.0,<2.17.0
onnx>=1.16.0
onnxruntime>=1.17.0

# ---- Cryptographic CLI dep (used by tools/cli_sealverify also) --------------
cryptography>=42.0.0                  # ECDSA-P256 sign/verify in Python for the CLI
jcs>=0.2.1                            # RFC 8785 JSON canonicalization (companion to Kotlin lib)
piexif>=1.1.3                         # EXIF UserComment read/write
pyjwt[crypto]>=2.8.0                  # for key handling helpers (optional)

# ---- Tests + dev quality ----------------------------------------------------
pytest>=8.0.0
pytest-cov>=4.1.0
ruff>=0.4.0
mypy>=1.9.0
